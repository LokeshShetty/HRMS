import CustomCard from "../../../../components/CustomCard/CustomCard";

type Props = {};

const AttendanceRecord = (props: Props) => {
  return (
    <CustomCard cardTitle="Attendance Records">
      <div>hello</div>
    </CustomCard>
  );
};

export default AttendanceRecord;
