import styled from "styled-components";

export const LeaveDays = styled.div`
  background: linear-gradient(180deg, #fa6e65 0%, #ff9d2a 100%);
  background-clip: text;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  font-size: 26px;
  font-style: normal;
  font-weight: 600;
  line-height: 29px; /* 92.308% */
`;

export const LeaveDescription = styled.div`
  color: var(--primary-color);
  font-size: 12px;
  font-style: normal;
  font-weight: 600;
  line-height: normal;
`;

export const MainDiv = styled.div`
  display: flex;
  flex-direction: column;
  gap: 8px;
`;
